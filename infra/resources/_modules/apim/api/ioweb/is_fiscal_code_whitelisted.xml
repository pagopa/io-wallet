<policies>
    <inbound>
        <base />
        <set-method>GET</set-method>
        <rewrite-uri template="@($"/whitelisted-fiscal-code/{context.Variables["fiscalCode"]}")" />
    </inbound>
    <outbound>
        <base />
        <set-body>@{
            var response = context.Response.Body.As<JObject>(preserveContent: true);
            var outBody = new JObject();
            outBody["is_whitelisted"] = response["whitelisted"];
            return outBody.ToString();
        }</set-body>
    </outbound>
</policies>