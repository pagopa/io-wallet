<policies>
    <inbound>
        <base />
        <set-method>GET</set-method>
        <set-header name="fiscal-code" exists-action="override">
            <value>@((string)context.Variables["fiscalCode"])</value>
        </set-header>
    </inbound>
    <outbound>
        <base />
        <set-body>@{
            var res = context.Response.Body.As<JObject>(true);
            return new JObject(new JProperty("id", res["id"]), new JProperty("is_revoked", res["is_revoked"])).ToString();
        }</set-body>
    </outbound>
</policies>