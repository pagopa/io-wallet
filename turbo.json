{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"],
      "outputLogs": "errors-only",
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "inputs": ["src/**/*.ts"],
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "format": {},
    "lint": {
      "inputs": ["src/**/*.ts"],
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "lint:fix": {
      "inputs": ["src/**/*.ts"],
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "test": {
      "inputs": ["src/**/*.spec.ts"],
      "outputs": [],
      "outputLogs": "errors-only",
      "dependsOn": ["^build"]
    }
  }
}
